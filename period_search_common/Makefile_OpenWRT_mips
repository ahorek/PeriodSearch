# Define compiler and flags
STAGING_DIR = ../../openwrt/staging_dir/toolchain-mips_mips32_gcc-13.3.0_musl

CXX = $(STAGING_DIR)/bin/mips-openwrt-linux-musl-g++
CXXFLAGS = -march=mips32 -O3 -std=c++2a -w -s 

# Define the target library
TARGET = build/libps_common.a

# Define the source files
SRC = arrayHelpers.cpp dot_product.cpp LcHelpers.cpp

# Define the object files
OBJ = $(SRC:%.cpp=build/%.o)

# Default target to build the library
all: $(TARGET)

# Rule to build the static library
$(TARGET): $(OBJ)
	ar rcs $@ $^

# Rule to build object files
build/%.o: %.cpp
	@mkdir -p build
	STAGING_DIR=$(STAGING_DIR) $(CXX) $(CXXFLAGS) -c $< -o $@

# Clean rule to remove compiled files
clean:
	rm -f $(OBJ) $(TARGET)

#	rm -rf build

.PHONY: all clean
